# SUPERSEDED — all configuration is in application.yaml
# This file is intentionally empty to prevent dual-loading conflicts.
# Spring Boot loads both .yml and .yaml; application.yaml is the canonical source.
