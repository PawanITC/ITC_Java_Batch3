pipeline {
    agent any

    stages {
        stage('Test') {
            steps {
                echo 'test change'
                sh 'cd backend/catalogue-service && ./gradlew test'
            }
        }
    }
}