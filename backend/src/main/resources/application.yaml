spring:
  application:
    name: funkart

  datasource:
    url: jdbc:h2:mem:funkart
    driver-class-name: org.h2.Driver
    username: sa
    password: password

  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    hibernate:
      ddl-auto: update
    show-sql: true
