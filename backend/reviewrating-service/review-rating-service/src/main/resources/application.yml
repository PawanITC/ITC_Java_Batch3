spring:
  datasource:
    url: jdbc:postgresql://18.175.222.132:32680/postgres
    username: postgres
    password: Ie422p9Cx8
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  security:
    oauth2:
      resourceserver:
        jwt:
          secret-key: your256bitsecretkeymustbeatleast32chars!!

  kafka:
    bootstrap-servers: localhost:9092
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      properties:
        enable.idempotence: true
        acks: all
        retries: 10
        max.in.flight.requests.per.connection: 5
        spring.json.add.type.headers: false
      template:
        default-topic: review.events.v1

  management:
    endpoints:
      web:
        exposure:
          include: health,info,prometheus

  resilience4j:
    circuitbreaker:
      instances:
        userService:
          registerHealthIndicator: true
          slidingWindowSize: 10
          minimumNumberOfCalls: 5
          failureRateThreshold: 50
          waitDurationInOpenState: 10s
          permittedNumberOfCallsInHalfOpenState: 3

    retry:
      instances:
        userService:
          maxAttempts: 3
          waitDuration: 500ms

      timelimiter:
        instances:
          userService:
            timeoutDuration: 2s

rating-aggregator:
  base-url: http://localhost:9096/aggregator

app:
  kafka:
    topic:
      review-created: review.events.v1





